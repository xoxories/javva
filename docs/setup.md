# Setup

> TODO: document local setup — prerequisites, environment variables, running the backend (and later, the frontend), and any one-time steps such as downloading the spaCy model used by Presidio.
